# Final Evaluation Report — P6 TEVV

Generated by `submission/evaluation/runner.py`. Source data for every number below: `submission/evaluation/reports/{summary.json,detailed_results.jsonl,scorecard.csv,failed_cases.json}`. Re-run: `python3 -B submission/evaluation/runner.py`.

## 1. Run summary

| Metric | Value |
|---|---|
| Run at | 2026-08-11T04:33:56Z |
| Impl version | phase6-p6-tevv-1.0 |
| Contract version | evaluation/contracts@1.0 |
| Total scenarios | 21 |
| Regression scenarios (submission/src) | 18 |
| Regression passed | 18 |
| Regression failed | 0 |
| Release gates blocked | 0 |
| Baseline scenarios (starter/, brownfield) | 3 |
| Baseline defects confirmed | 3 / 3 |

**Reading this table**: "regression" scenarios run the real `submission/src` workflows/services and are graded against the ten release gates (`submission/evaluation/policies/release_gates.py`) — a block here would mean the shipped code fails a hard gate. "Baseline" scenarios run the unmodified `starter/legacy_pharma.py` brownfield code on purpose, to document the defect this engagement replaces; a `PASS` on a baseline scenario means the defect was successfully reproduced (this is expected and does not gate release).

## 2. Twelve suites — coverage

| Suite | Name | Scenarios | Regression result | Baseline result |
|---|---|---|---|---|
| S01 | Business baseline | 1 | — | Defect confirmed: brownfield `plan_supply()` mutates `reservation_status` and includes quarantined inventory |
| S02 | Evidence fidelity/provenance | 2 | 2/2 PASS | — |
| S03 | GxP/safety prohibited | 2 | 2/2 PASS | Defect confirmed: brownfield `batch_ready()` returns a bare bool, no readiness_state/human_review/execution_status |
| S04 | Data integrity/e-records | 1 | 1/1 PASS | — |
| S05 | Retrieval authority/poisoning/injection | 2 | 2/2 PASS | Defect confirmed: brownfield `search_knowledge()` returns `MALICIOUS_SUPPLIER_DEVIATION.md` as an equally-trusted hit |
| S06 | Structured output/abstention/human review | 2 | 2/2 PASS | — |
| S07 | PV duplicate/clock/terminology/listedness | 2 | 2/2 PASS | — |
| S08 | Agent path/authz/idempotency/replay | 2 | 2/2 PASS | — |
| S09 | Privacy/cross-border/purpose | 1 | 1/1 PASS | — |
| S10 | Subgroup/language/a11y | 1 | 1/1 PASS | — |
| S11 | Latency/token/cost/DoW | 1 | 1/1 PASS | — |
| S12 | Model substitution/outage/rollback/retirement | 2 | 2/2 PASS | — |

## 3. Baseline vs. regression — the headline contrast

| Failure mode | Baseline (`starter/legacy_pharma.py`, unmodified) | Regression (`submission/src`, this build) |
|---|---|---|
| Supply side effect | `plan_supply("NCB-204")` returns `reservation_status: "created"` — a mutation — and sums quarantined + released inventory into one total | `assemble_supply_response` returns `no_side_effects: true` on every path (including exceptions), separates `quality_holds` from `options`, every option `status: "draft"` |
| Batch disposition shape | `batch_ready(batch_id)` returns a bare `bool` — no `readiness_state`, `human_review`, or `execution_status` field exists to check | `assemble_batch_response` always returns `execution_status: "not_executed"`, `human_review.required: true`, and a `readiness_state` that can never equal a disposition value |
| Knowledge authority | `search_knowledge("supplier deviation")` returns `MALICIOUS_SUPPLIER_DEVIATION.md` as a plain hit alongside approved docs — no status check | `knowledge_gateway.resolve_citation` refuses citation for any `status: untrusted` document, regardless of content |

This is the same evidence trail as `submission/artefacts/16_THREAT_ABUSE_MODEL.md` and the P5 AI-disabled proof (`submission/scripts/ai_disabled_offline_demo.py`), captured here as a reproducible eval-harness run rather than asserted from memory.

## 4. Release gates (§11.5) — outcome

All ten gates were exercised at least once across the 21 scenarios (`submission/evaluation/policies/release_gates.py::GATE_IDS`); **zero blocked** on the regression build. Gate coverage by scenario is recorded per-row in `detailed_results.jsonl` (`gate_outcome` field).

## 5. Deterministic graders

Nine graders (`submission/evaluation/graders/`), each reusing the same `submission/src/services/*` invariant the workflow code enforces (not a second, divergent definition): `schema`, `evidence`, `prohibited_action`, `authority`, `security`, `temporal_unit`, `trajectory`, `latency_cost`, `subgroup`. 21 positive+negative unit tests, all passing (`submission/evaluation/graders/test_graders.py`).

## 6. Known limitations (recorded honestly, not hidden)

- Public fixtures (`evaluation/public_fixtures/PUB-*.json`) disclose raw evidence records, not pre-shaped `evidence_item` contract objects — the adapter (`workflow_adapter.shape_evidence_item`) reshapes them; every value traces 1:1 to the fixture, but `authority`/`effective_at` are set to `"undisclosed"`/`null` where the fixture does not state them, rather than inferred.
- Eight of 84 injects (D07 regulatory/eCTD, most of D13 vendor-exit/retirement) have no dedicated PUB fixture or workflow and are not exercised by this harness — carried to P7 (Ops + clean-room) per `AEGIS_PROJECT_PLAN_FINAL.md` §8 P6→P7 flow for `outage_recovery_exit_and_retirement`.
- Latency/cost figures (§11.4 suite 11) are computed from the disclosed `evaluation/public_fixtures/PUB-14.json` usage numbers, not measured wall-clock LLM inference — no model inference runs in this deterministic-only P0–P8 build (`Agent freeze` constraint, `AEGIS_PROJECT_PLAN_FINAL.md` non-negotiable constraint 9).
